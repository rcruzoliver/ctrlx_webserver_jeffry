# automatically generated by the FlatBuffers compiler, do not modify

# namespace: datalayer

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ArrayOfDouble(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ArrayOfDouble()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsArrayOfDouble(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ArrayOfDouble
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ArrayOfDouble
    def Value(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Float64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ArrayOfDouble
    def ValueAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Float64Flags, o)
        return 0

    # ArrayOfDouble
    def ValueLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ArrayOfDouble
    def ValueIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def ArrayOfDoubleStart(builder): builder.StartObject(1)
def Start(builder):
    return ArrayOfDoubleStart(builder)
def ArrayOfDoubleAddValue(builder, value): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(value), 0)
def AddValue(builder, value):
    return ArrayOfDoubleAddValue(builder, value)
def ArrayOfDoubleStartValueVector(builder, numElems): return builder.StartVector(8, numElems, 8)
def StartValueVector(builder, numElems):
    return ArrayOfDoubleStartValueVector(builder, numElems)
def ArrayOfDoubleEnd(builder): return builder.EndObject()
def End(builder):
    return ArrayOfDoubleEnd(builder)
try:
    from typing import List
except:
    pass

class ArrayOfDoubleT(object):

    # ArrayOfDoubleT
    def __init__(self):
        self.value = None  # type: List[float]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        arrayOfDouble = ArrayOfDouble()
        arrayOfDouble.Init(buf, pos)
        return cls.InitFromObj(arrayOfDouble)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, arrayOfDouble):
        x = ArrayOfDoubleT()
        x._UnPack(arrayOfDouble)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.value == other.value

    # ArrayOfDoubleT
    def _UnPack(self, arrayOfDouble):
        if arrayOfDouble is None:
            return
        if not arrayOfDouble.ValueIsNone():
            if np is None:
                self.value = []
                for i in range(arrayOfDouble.ValueLength()):
                    self.value.append(arrayOfDouble.Value(i))
            else:
                self.value = arrayOfDouble.ValueAsNumpy()

    # ArrayOfDoubleT
    def Pack(self, builder):
        if self.value is not None:
            if np is not None and type(self.value) is np.ndarray:
                value = builder.CreateNumpyVector(self.value)
            else:
                ArrayOfDoubleStartValueVector(builder, len(self.value))
                for i in reversed(range(len(self.value))):
                    builder.PrependFloat64(self.value[i])
                value = builder.EndVector()
        ArrayOfDoubleStart(builder)
        if self.value is not None:
            ArrayOfDoubleAddValue(builder, value)
        arrayOfDouble = ArrayOfDoubleEnd(builder)
        return arrayOfDouble
