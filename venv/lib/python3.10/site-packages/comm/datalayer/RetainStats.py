# automatically generated by the FlatBuffers compiler, do not modify

# namespace: datalayer

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class RetainStats(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RetainStats()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRetainStats(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # RetainStats
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # total size of memory in bytes
    # RetainStats
    def Total(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # free size of memory in bytes
    # RetainStats
    def Free(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # used size of memory in bytes
    # RetainStats
    def Used(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # biggest free chunk of memory in bytes
    # RetainStats
    def BiggestFree(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # now often was the nvram synced
    # RetainStats
    def SyncCounter(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # last used offset
    # RetainStats
    def LastUsed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # debug information of shared memory
    # RetainStats
    def Info(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # diagnosis of nvram
    # RetainStats
    def Diagnosis(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from comm.datalayer.RetainDiagnosis import RetainDiagnosis
            obj = RetainDiagnosis()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def RetainStatsStart(builder): builder.StartObject(8)
def Start(builder):
    return RetainStatsStart(builder)
def RetainStatsAddTotal(builder, total): builder.PrependUint32Slot(0, total, 0)
def AddTotal(builder, total):
    return RetainStatsAddTotal(builder, total)
def RetainStatsAddFree(builder, free): builder.PrependUint32Slot(1, free, 0)
def AddFree(builder, free):
    return RetainStatsAddFree(builder, free)
def RetainStatsAddUsed(builder, used): builder.PrependUint32Slot(2, used, 0)
def AddUsed(builder, used):
    return RetainStatsAddUsed(builder, used)
def RetainStatsAddBiggestFree(builder, biggestFree): builder.PrependUint32Slot(3, biggestFree, 0)
def AddBiggestFree(builder, biggestFree):
    return RetainStatsAddBiggestFree(builder, biggestFree)
def RetainStatsAddSyncCounter(builder, syncCounter): builder.PrependUint32Slot(4, syncCounter, 0)
def AddSyncCounter(builder, syncCounter):
    return RetainStatsAddSyncCounter(builder, syncCounter)
def RetainStatsAddLastUsed(builder, lastUsed): builder.PrependUint32Slot(5, lastUsed, 0)
def AddLastUsed(builder, lastUsed):
    return RetainStatsAddLastUsed(builder, lastUsed)
def RetainStatsAddInfo(builder, info): builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(info), 0)
def AddInfo(builder, info):
    return RetainStatsAddInfo(builder, info)
def RetainStatsAddDiagnosis(builder, diagnosis): builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(diagnosis), 0)
def AddDiagnosis(builder, diagnosis):
    return RetainStatsAddDiagnosis(builder, diagnosis)
def RetainStatsEnd(builder): return builder.EndObject()
def End(builder):
    return RetainStatsEnd(builder)
import comm.datalayer.RetainDiagnosis
try:
    from typing import Optional
except:
    pass

class RetainStatsT(object):

    # RetainStatsT
    def __init__(self):
        self.total = 0  # type: int
        self.free = 0  # type: int
        self.used = 0  # type: int
        self.biggestFree = 0  # type: int
        self.syncCounter = 0  # type: int
        self.lastUsed = 0  # type: int
        self.info = None  # type: str
        self.diagnosis = None  # type: Optional[comm.datalayer.RetainDiagnosis.RetainDiagnosisT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        retainStats = RetainStats()
        retainStats.Init(buf, pos)
        return cls.InitFromObj(retainStats)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, retainStats):
        x = RetainStatsT()
        x._UnPack(retainStats)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.total == other.total and \
            self.free == other.free and \
            self.used == other.used and \
            self.biggestFree == other.biggestFree and \
            self.syncCounter == other.syncCounter and \
            self.lastUsed == other.lastUsed and \
            self.info == other.info and \
            self.diagnosis == other.diagnosis

    # RetainStatsT
    def _UnPack(self, retainStats):
        if retainStats is None:
            return
        self.total = retainStats.Total()
        self.free = retainStats.Free()
        self.used = retainStats.Used()
        self.biggestFree = retainStats.BiggestFree()
        self.syncCounter = retainStats.SyncCounter()
        self.lastUsed = retainStats.LastUsed()
        self.info = retainStats.Info()
        if retainStats.Diagnosis() is not None:
            self.diagnosis = comm.datalayer.RetainDiagnosis.RetainDiagnosisT.InitFromObj(retainStats.Diagnosis())

    # RetainStatsT
    def Pack(self, builder):
        if self.info is not None:
            info = builder.CreateString(self.info)
        if self.diagnosis is not None:
            diagnosis = self.diagnosis.Pack(builder)
        RetainStatsStart(builder)
        RetainStatsAddTotal(builder, self.total)
        RetainStatsAddFree(builder, self.free)
        RetainStatsAddUsed(builder, self.used)
        RetainStatsAddBiggestFree(builder, self.biggestFree)
        RetainStatsAddSyncCounter(builder, self.syncCounter)
        RetainStatsAddLastUsed(builder, self.lastUsed)
        if self.info is not None:
            RetainStatsAddInfo(builder, info)
        if self.diagnosis is not None:
            RetainStatsAddDiagnosis(builder, diagnosis)
        retainStats = RetainStatsEnd(builder)
        return retainStats
