# automatically generated by the FlatBuffers compiler, do not modify

# namespace: networkmanager

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class Interfaces(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Interfaces()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsInterfaces(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Interfaces
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Interfaces
    def Interfaces(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from networkmanager.Interface import Interface
            obj = Interface()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Interfaces
    def InterfacesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Interfaces
    def InterfacesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def InterfacesStart(builder): builder.StartObject(1)
def Start(builder):
    return InterfacesStart(builder)
def InterfacesAddInterfaces(builder, interfaces): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(interfaces), 0)
def AddInterfaces(builder, interfaces):
    return InterfacesAddInterfaces(builder, interfaces)
def InterfacesStartInterfacesVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartInterfacesVector(builder, numElems):
    return InterfacesStartInterfacesVector(builder, numElems)
def InterfacesEnd(builder): return builder.EndObject()
def End(builder):
    return InterfacesEnd(builder)
import networkmanager.Interface
try:
    from typing import List
except:
    pass

class InterfacesT(object):

    # InterfacesT
    def __init__(self):
        self.interfaces = None  # type: List[networkmanager.Interface.InterfaceT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        interfaces = Interfaces()
        interfaces.Init(buf, pos)
        return cls.InitFromObj(interfaces)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, interfaces):
        x = InterfacesT()
        x._UnPack(interfaces)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.interfaces == other.interfaces

    # InterfacesT
    def _UnPack(self, interfaces):
        if interfaces is None:
            return
        if not interfaces.InterfacesIsNone():
            self.interfaces = []
            for i in range(interfaces.InterfacesLength()):
                if interfaces.Interfaces(i) is None:
                    self.interfaces.append(None)
                else:
                    interface_ = networkmanager.Interface.InterfaceT.InitFromObj(interfaces.Interfaces(i))
                    self.interfaces.append(interface_)

    # InterfacesT
    def Pack(self, builder):
        if self.interfaces is not None:
            interfaceslist = []
            for i in range(len(self.interfaces)):
                interfaceslist.append(self.interfaces[i].Pack(builder))
            InterfacesStartInterfacesVector(builder, len(self.interfaces))
            for i in reversed(range(len(self.interfaces))):
                builder.PrependUOffsetTRelative(interfaceslist[i])
            interfaces = builder.EndVector()
        InterfacesStart(builder)
        if self.interfaces is not None:
            InterfacesAddInterfaces(builder, interfaces)
        interfaces = InterfacesEnd(builder)
        return interfaces
