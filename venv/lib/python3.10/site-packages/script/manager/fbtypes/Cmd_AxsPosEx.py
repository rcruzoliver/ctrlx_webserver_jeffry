# automatically generated by the FlatBuffers compiler, do not modify

# namespace: fbtypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class Cmd_AxsPosEx(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Cmd_AxsPosEx()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCmd_AxsPosEx(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Cmd_AxsPosEx
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Cmd_AxsPosEx
    def Base(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from script.manager.fbtypes.Cmd_Base import Cmd_Base
            obj = Cmd_Base()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Cmd_AxsPosEx
    def Pos(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Cmd_AxsPosEx
    def Buffered(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Cmd_AxsPosEx
    def Lim(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from script.manager.fbtypes.Cmd_DynLimits import Cmd_DynLimits
            obj = Cmd_DynLimits()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Selected direction for PosAbs with modulo axis (ignored in all other cases)
    # Cmd_AxsPosEx
    def Direction(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

def Cmd_AxsPosExStart(builder): builder.StartObject(5)
def Start(builder):
    return Cmd_AxsPosExStart(builder)
def Cmd_AxsPosExAddBase(builder, base): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(base), 0)
def AddBase(builder, base):
    return Cmd_AxsPosExAddBase(builder, base)
def Cmd_AxsPosExAddPos(builder, pos): builder.PrependFloat64Slot(1, pos, 0.0)
def AddPos(builder, pos):
    return Cmd_AxsPosExAddPos(builder, pos)
def Cmd_AxsPosExAddBuffered(builder, buffered): builder.PrependBoolSlot(2, buffered, 0)
def AddBuffered(builder, buffered):
    return Cmd_AxsPosExAddBuffered(builder, buffered)
def Cmd_AxsPosExAddLim(builder, lim): builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(lim), 0)
def AddLim(builder, lim):
    return Cmd_AxsPosExAddLim(builder, lim)
def Cmd_AxsPosExAddDirection(builder, direction): builder.PrependInt8Slot(4, direction, 0)
def AddDirection(builder, direction):
    return Cmd_AxsPosExAddDirection(builder, direction)
def Cmd_AxsPosExEnd(builder): return builder.EndObject()
def End(builder):
    return Cmd_AxsPosExEnd(builder)
import script.manager.fbtypes.Cmd_Base
import script.manager.fbtypes.Cmd_DynLimits
try:
    from typing import Optional
except:
    pass

class Cmd_AxsPosExT(object):

    # Cmd_AxsPosExT
    def __init__(self):
        self.base = None  # type: Optional[script.manager.fbtypes.Cmd_Base.Cmd_BaseT]
        self.pos = 0.0  # type: float
        self.buffered = False  # type: bool
        self.lim = None  # type: Optional[script.manager.fbtypes.Cmd_DynLimits.Cmd_DynLimitsT]
        self.direction = 0  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        cmdAxsPosEx = Cmd_AxsPosEx()
        cmdAxsPosEx.Init(buf, pos)
        return cls.InitFromObj(cmdAxsPosEx)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, cmdAxsPosEx):
        x = Cmd_AxsPosExT()
        x._UnPack(cmdAxsPosEx)
        return x

    def __eq__(self, other):
        return type(self) == type(other) and \
            self.base == other.base and \
            self.pos == other.pos and \
            self.buffered == other.buffered and \
            self.lim == other.lim and \
            self.direction == other.direction

    # Cmd_AxsPosExT
    def _UnPack(self, cmdAxsPosEx):
        if cmdAxsPosEx is None:
            return
        if cmdAxsPosEx.Base() is not None:
            self.base = script.manager.fbtypes.Cmd_Base.Cmd_BaseT.InitFromObj(cmdAxsPosEx.Base())
        self.pos = cmdAxsPosEx.Pos()
        self.buffered = cmdAxsPosEx.Buffered()
        if cmdAxsPosEx.Lim() is not None:
            self.lim = script.manager.fbtypes.Cmd_DynLimits.Cmd_DynLimitsT.InitFromObj(cmdAxsPosEx.Lim())
        self.direction = cmdAxsPosEx.Direction()

    # Cmd_AxsPosExT
    def Pack(self, builder):
        if self.base is not None:
            base = self.base.Pack(builder)
        if self.lim is not None:
            lim = self.lim.Pack(builder)
        Cmd_AxsPosExStart(builder)
        if self.base is not None:
            Cmd_AxsPosExAddBase(builder, base)
        Cmd_AxsPosExAddPos(builder, self.pos)
        Cmd_AxsPosExAddBuffered(builder, self.buffered)
        if self.lim is not None:
            Cmd_AxsPosExAddLim(builder, lim)
        Cmd_AxsPosExAddDirection(builder, self.direction)
        cmdAxsPosEx = Cmd_AxsPosExEnd(builder)
        return cmdAxsPosEx
